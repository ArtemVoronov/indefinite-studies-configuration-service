apiVersion: apps/v1
kind: Deployment
metadata:
  name: indefinite-studies-auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: indefinite-studies-auth-service
  template:
    metadata:
      labels:
        app: indefinite-studies-auth-service
    spec:
      containers:
        - name: indefinite-studies-auth-service
          image: indefinite-studies-auth-service:{{ .Values.global.version.auth }}
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: {{ .Values.global.ports.rest }}
              name: rest-port
            - containerPort: {{ .Values.global.ports.grpc }}
              name: grpc-port     
          volumeMounts:
            - name: log-output
              mountPath: /tmp/
        - name: auth-beat-auto
          image: elastic/filebeat:7.8.0
          env:
            - name: LOGSTASH_HOSTS
              value: {{ .Values.global.hostIp }}:{{ .Values.global.ports.logstash }}
          args: [
            "-c", "/etc/filebeat/filebeat.yml",
            "-e"
          ]
          volumeMounts:
          - name: log-output
            mountPath: /tmp/
          - name: auth-beat-config
            mountPath: /etc/filebeat/
      initContainers:
        - name: indefinite-studies-auth-service-liquibase
          image: indefinite-studies-auth-service-liquibase:{{ .Values.global.version.auth }}
          env:
            - name: DATABASE_USER
              value: "indefinite_studies_auth_service_user"
            - name: DATABASE_PASSWORD
              value: "password"
            - name: DATABASE_URL
              value: "jdbc:postgresql://indefinite-studies-auth-service-postgres:5432/indefinite_studies_auth_service_db"
      volumes:
        - name: log-output
          emptyDir: {}
        - name: auth-beat-config
          configMap:
            name: auth-beat-config
            items:
              - key: filebeat.yml
                path: filebeat.yml